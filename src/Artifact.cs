namespace DefaultNamespace;

public class Artifact
{
    // Distance formula for circle to detect collision
    // Compare itself and input of player.
    // x, y, radius
    // Player's x, y, and radius are parameters
    int x1;
    int y1;
    int x2;
    int y2;
    
    Math.Sqrt((x2 - x1)^2 + (y2 - y1)^2) - (r2 + r1)
}